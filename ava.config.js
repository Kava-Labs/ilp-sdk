export default {
  files: ['build/__tests__/**/*.js'],
  failFast: true,
  verbose: true,
  serial: true,
  timeout: '3m'
  // compileEnhancements: false,
  // extensions: ['ts']
  // require: ['ts-node/register']
}
